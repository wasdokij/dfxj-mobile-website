import IndexInvite from 'components/index/index-invite.vue';
import '../lib/layer.js';
import '../lib/layer.css';

var V = new Vue({
    el: '#container',
    components: {
        IndexInvite
    },
});
